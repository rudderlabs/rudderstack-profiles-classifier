from setuptools import setup, find_packages

setup(
    name="profiles_rudderstack_classifier",
    version="0.1.0",
    author="RudderStack",
    description="TODO",
    packages=find_packages(),
    install_requires=[],
)
